import React from 'react';
import * as THREE from 'three';

interface SceneObjectParam {
  object: THREE.Object3D;
}

const SceneObject: React.FC<SceneObjectParam> = () => {
  return null;
};

export default SceneObject;
